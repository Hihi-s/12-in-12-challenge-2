<%= form_for ([@post, @post.comments.build]) do |f| %>
  <p>
  <%= f.label :name %><br>
  <%= f.text_field :name %>
  </p>

  <p>
  <%= f.label :comment %><br>
  <%= f.text_area :body %>
  </p>

  <br>
  <p>
  <%= f.submit "Create comment", :disable_with => 'Submiting...' %>
  </p>
<% end %>